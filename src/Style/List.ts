import { StyleSheet } from 'react-native';

import { variable } from './variable';

export const list = StyleSheet.create({
    backgroundEven: {
        backgroundColor: variable.colorGrayLight2
    },
    backgroundOdd: {
        backgroundColor: variable.colorGrayLight3
    }
});
